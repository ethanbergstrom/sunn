version: 0.1
component: build
steps:
  - type: Command
    timeoutInSeconds: 1200
    name: "Build Function into container image"
    command: |
      fn build --verbose

outputArtifacts:
  - name: output01
    type: DOCKER_IMAGE
    # this location tag doesn't effect the tag used to deliver the container image
    # to the Container Registry
    location: enviro:latest